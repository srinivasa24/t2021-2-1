#include <stdio.h>
void main()
{
   int i,a,sum=0;

   printf("Input a : ");
   scanf("%d",&a);
   printf("\nThen output :");
   for(i=1;i<=a;i++)
   {
     printf("%d ",2*i-1);
     sum+=2*i-1;
   }
}
